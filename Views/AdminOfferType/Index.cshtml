@model IEnumerable<Paris2024.Models.OfferType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OfferType_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OfferType_AllowedPerson)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OfferType_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OfferType_AllowedPerson)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OfferTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OfferTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OfferTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
